/**
 * @name 虚拟节点 Virtual DOM
 *
 * @class VD
 *
 * @constructor
 * @param {DOM} prefix 类名前缀
 *
 * @attr {string} className 类名
 *
 * @method set 设置要处理的节点列表
 * @param {DOM Object Array} nodes 要处理的节点列表
 * @return {DOM Object Array} 要处理的节点列表
 *
 */

export class VD {
  constructor(dom) {
    console.log(dom);
  }

  // emptyFirstPageNodes() {
  //   this._firstPageNodes = [];
  // }
};
